# Secrets / instance config — never commit (keep real values in your private infra repo)
config.env
.env

# Local-only working area — planning docs, design notes, scratch. Never published.
.dev/

# Personal local overrides — never published
*.local.md

# Python
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.venv/
